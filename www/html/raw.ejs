<% for (var mod of mods) { %><%= mod.id %> - <%= mod.domain %>/<%= mod.nexusid %> (<%= mod.name %>)<br><% } %><br><br>
<% for (var author of authors) { %><%= author.id %> - <%= author.discordids %> (<%= author.name %>)<br><% } %><br><br>
<% for (var vote of votes) { %><%= vote %><br><% } %>